# Generated Drama Posters

This folder is written by `scripts/build_drama_posters.ps1`.

Each drama gets two generated files:

- `*_card.jpg` for the home drama cards.
- `*_history.jpg` for the recently watched list.

The frontend falls back to the existing generated cover UI if a poster file is missing.
